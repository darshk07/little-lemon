import React from 'react'
import { Nav } from './nav'

export const Header = () => {
    return (
        <header>
            <img src="logo.svg" alt="" />
            <Nav />
        </header>
    )
}
